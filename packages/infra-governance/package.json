{
  "name": "@jataqi/infra-governance",
  "version": "0.1.0",
  "description": "JATA Qi Secure Infrastructure Governance — hardware lifecycle management (inventory, provisioning, EOL/decommission), trusted provisioning + secure firmware validation (expected hashes, measured-boot attestation), infrastructure integrity monitoring (config drift, compliance checks), and physical security controls (access records, visitor logs)",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "clean": "rm -rf dist",
    "test": "node --test dist/test/*.test.js",
    "pretest": "npm run build"
  },
  "files": ["dist", "src"],
  "license": "MIT",
  "dependencies": {
    "@jataqi/core-kernel": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
