{
  "name": "@jataqi/supply-chain-security",
  "version": "0.1.0",
  "description": "JATA Qi Software Supply Chain Governance — source repository validation (protected branches, signed commits), CI/CD pipeline validation (pinned steps, approval gates), dependency integrity (lockfile hashing, CVE audit, license audit), SLSA-aligned artifact provenance (builder/material hashes, signature verification), release signing, deployment verification, and continuous integrity monitoring",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "clean": "rm -rf dist",
    "test": "node --test dist/test/*.test.js",
    "pretest": "npm run build"
  },
  "files": ["dist", "src"],
  "license": "MIT",
  "dependencies": {
    "@jataqi/core-kernel": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
