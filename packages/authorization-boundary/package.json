{
  "name": "@jataqi/authorization-boundary",
  "version": "0.1.0",
  "description": "JATA Qi A-01 — the single authoritative fail-closed authorization boundary: sealed tamper-resistant authorization envelopes, explicit capability manifests, a policy decision point, a credential broker with fail-closed semantics, replay protection, exact approval/action binding, budget/rate enforcement, durable privacy-safe provenance, and enforced checkpoints for agent-initiated externally consequential invocations",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "clean": "rm -rf dist",
    "test": "node --test dist/test/*.test.js",
    "pretest": "npm run build"
  },
  "files": ["dist", "src"],
  "license": "MIT",
  "dependencies": {
    "@jataqi/authentication": "0.1.0",
    "@jataqi/commercial-control-plane": "0.1.0",
    "@jataqi/core-kernel": "0.1.0",
    "@jataqi/storage": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
