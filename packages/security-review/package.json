{
  "name": "@jataqi/security-review",
  "version": "0.1.0",
  "description": "JATA Qi Independent Security Review — design/architecture security assessments, secure code review, infrastructure review, AI safety review, compliance assessment (ISO 27001 / SOC 2 alignment), and periodic independent audits with severity-rated findings, remediation tracking, and sign-off workflows",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "clean": "rm -rf dist",
    "test": "node --test dist/test/*.test.js",
    "pretest": "npm run build"
  },
  "files": ["dist", "src"],
  "license": "MIT",
  "dependencies": {
    "@jataqi/core-kernel": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
