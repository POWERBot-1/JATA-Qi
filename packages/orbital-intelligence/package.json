{
  "name": "@jataqi/orbital-intelligence",
  "version": "0.1.0",
  "description": "JATA Qi Orbital Intelligence Engine foundation — provider-neutral authorized-reference observation metadata, encrypted opaque local references, non-executing monitoring/request plans, spatiotemporal queries, world/temporal links, derived fusion/change assessment, and no provider/tasking/control capability",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "clean": "rm -rf dist",
    "test": "node --test dist/test/*.test.js",
    "pretest": "npm run build"
  },
  "files": ["dist", "src"],
  "license": "MIT",
  "dependencies": {
    "@jataqi/commercial-control-plane": "0.1.0",
    "@jataqi/core-kernel": "0.1.0",
    "@jataqi/storage": "0.1.0",
    "@jataqi/temporal-engine": "0.1.0",
    "@jataqi/world-model": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
