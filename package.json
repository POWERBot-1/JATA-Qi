{
  "name": "jataqi",
  "version": "0.1.0",
  "private": true,
  "description": "JATA Qi — Modular AI Operating System",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check:workspaces": "node scripts/check-workspace-lockfile.mjs",
    "prebuild": "npm run check:workspaces",
    "build": "node scripts/run-workspaces.mjs build",
    "pretest": "npm run check:workspaces",
    "test": "node scripts/run-workspaces.mjs test",
    "lint": "eslint packages scripts eslint.config.mjs",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules",
    "lint:fix": "eslint packages scripts --fix"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "typescript-eslint": "^8.69.0"
  }
}
